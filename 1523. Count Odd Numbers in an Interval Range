class Solution {
    public int countOdds(int low, int high) {
        // If both ends are even, odds are exactly half the interval
        // Otherwise, one extra odd exists.
        return ((high - low) / 2) + ((low % 2 == 1 || high % 2 == 1) ? 1 : 0);
    }
}
